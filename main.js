console.log(quickDatataset [randonNumber]);
sketch = quickDrawDataset [randowNumber];
timerCounter = 0;
timerCheck = [];
drawSketch = [];
ansewerHolder = [];
score = 0;
function draw() {
    strokeWeight(13);
    stroke(0);
    if (mouseIsPressed) {
        line(pmouseX, pmouseY, mouseX, mouseY);
    }
}












